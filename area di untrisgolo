#include <iostream.h>
#include <conio.h>
#include <math.h>          /riconosce la radice "sqrt"
main()
{
float a,b,c;
dato_entrata*/
float p,at;
dato_uscita*/
cout<< "inserisci il valore del lato a: cin>>a;
cout<< "inserisci il valore del lato b: cin>>b;
cout<< "inserisci il valore del lato c: cin>>c;
p==(a+b+c)/2.0;
at=sqr(p*(p-a)*(p-b)*(p*c));
cout<<at;
getch();
}
cout<<l'area del triangolo di <<"a"<<, <<"b"<<, <<"c"<< e':
<<at;
getch();
